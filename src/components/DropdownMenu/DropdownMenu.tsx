import DropdownMenu, { DropdownMenuProps as BootstrapDropdownMenuProps } from "react-bootstrap/DropdownMenu";

export default function DropdownMenuBootstrap (props: BootstrapDropdownMenuProps) {
    return (
        <DropdownMenu {...props}/>
    )
}